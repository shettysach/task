A few questions regarding this plan.
  1. We cannot directly import TextOp's high level parts as it is not a package, right? Or is there a way to do it, but will dependency mismatch affect the APIs anyway?
  2. So do we have to rewrite most of the code for this? What parts of TextOp can we actually reuse? Only the checkpoints and artifacts?
  3. "expand MDAR 23-DOF joints to 29 DOF". Any side effects from this expansion?
